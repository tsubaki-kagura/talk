logging:
  file:
    path: ./logs # 相对于项目根路径
    name: ${logging.file.path}/application.log
  logback:
    rollingpolicy:
      file-name-pattern: ${logging.file.path}/%d{yyyy-MM-dd}.%i.log
      max-file-size: 32MB
      max-history: 16 # 单位为天
  level:
    org.kagura: debug
    org.springframework:
      web.servlet:
        DispatcherServlet: debug
        mvc.method.annotation.RequestMappingHandlerMapping: debug

server:
  servlet:
    context-path: /api

spring:
  application:
    name: talk
  mvc:
    problemdetails:
      enabled: true
  security:
    login: /auth/login
    jwt:
      issuer: Kagura
      secret: ecaMKggYjYw2ozhYfdaRL/0kFxL3oKuBOWot/zQJpR0=
      expiration: 3600 # 有效时长，单位为秒
  jpa:
    open-in-view: false
    generate-ddl: true
    properties:
      hibernate:
        show-sql: true
        format_sql: true
  config:
    import:
      - optional:file:.env.local[.properties]
  datasource:
    url: jdbc:postgresql://${DATABASE_HOST}:${DATABASE_PORT}/${DATABASE}?reWriteBatchedInserts=true
    username: ${DATABASE_USERNAME}
    password: ${DATABASE_PASSWORD}
